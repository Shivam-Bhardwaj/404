# Baseline Snapshots

Captured locally via `./record-baseline.sh` on 2025-02-14 at 04:20 UTC while the backend was running in release mode. Values are representative only and should be replaced with fresh captures whenever simulation parameters change.

| File | Notes |
|------|-------|
| health.txt | Plain-text `OK` response from `/health` |
| gpu-info.json | JSON body from `/api/gpu-info` |
| simulate-boids.json | Abbreviated payload from `/api/simulate/boids` |
| simulate-sph.json | Abbreviated payload from `/api/simulate/sph` |
| simulate-grayscott.json | Abbreviated payload from `/api/simulate/grayscott` |
